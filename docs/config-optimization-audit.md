# 三/四机场配置设计与专项验收

文件发布整理：三机场主配置为`configfull_new.yaml`，四机场为`cinfigfull_new_4.yaml`（按用户指定拼写）；`configfull.bak.yaml`逐字节保存本次获取的origin/main原配置。以下保留各轮历史状态，过去的“正式配置未切换”描述不代表本次文件提升后的状态。设备仍未部署。

日期：2026-09-11。官方Mihomo v1.19.30。当前候选为[三机场配置](../configfull_new.yaml)，未来家宽使用完整的[四机场模板](../cinfigfull_new_4.yaml)。每次只加载一份，正式`configfull_new.yaml`与设备配置未切换。业务归属见[设计说明](single-config-design.md)，分层理由见[分组依据](business-grouping-rationale.md)。

## 本轮：主配置提升与单提交整理

远程origin/main获取到cf13dc50，原configfull_new.yaml逐字节保存为configfull.bak.yaml（SHA256：5ded0094006bb6f97d60ceaedb64da80727185a583ddc7269412aa01153d962c）。两份设计分别提升为configfull_new.yaml、cinfigfull_new_4.yaml；完整YAML展开与改名前相同，只补充已验证内核版本说明。脚本、文档引用与配置校验同步迁移，旧AI/下载测试入口复用当前回归，清除旧架构专用断言。

新文件名下三/四机场静态及原生配置校验通过，各7类生命周期回归通过；635个YAML、632个MRS规则文件校验通过。旧CI固定v1.19.19明确报不支持rematch，固定验收改为已验证的v1.19.30，并保留最新稳定版检查；不将v1.19.19兼容失败标为通过。

保留远程4个规则更新提交，本地8个未推送提交压缩到远程最新main之上的单次提交。工作区快照、旧提交恢复分支和[本轮验证记录](/tmp/rules-promote-2emppsiz/manifest.json)可供回溯。设备尚未部署。

## 前轮：排版与无效定义清理

两份候选将参数、节点池和候选列表改为块式排版，简单业务归属保持单行，补齐探针/地区段落分隔，主规则注释统一缩进。移除两份未引用的region_eu、三机场未引用的exclude_download；仅引用一次的Fallback_Heartbeat_Base改为在实际组内声明interval/lazy。节点池中分别删除51/78处与继承模板重复的布尔声明。

完整展开对照确认，仅移除上述无运行用途的顶层定义，所有实际参数、规则顺序、默认、候选、分组名称和节点过滤完全相同。未发现孤立分组；规则集引用包含DNS用途。相同菜单的业务select仍需独立手选，CF/GitHub/通用池仍需独立健康状态，四层政策锚点仍有明确设计用途，因此保留。无新增分组、生成器或部署步骤。

两份均通过静态设计检查和官方Mihomo原文件配置检查，未重复与排版无关的故障矩阵。汇总脚本初次把内核成功日志关键字误写为successfully，核对日志后改为test is successful；两次内核检查本身均成功。正式配置与WRT未切换。[展开对照与验证记录](/tmp/rules-layout-j79lzbrn/manifest.json)。

## 前轮：AI与Tunnel开放主动覆盖

用户明确select应保留完整的人工选择能力。AI默认仍为机场名称1日本手选池，候选补齐机场名称1/3自动、五个地区链、共享家宽、DIRECT和全部实际节点；四机场文件额外提供机场名称2自动。Tunnel默认DIRECT，直接复用Direct_Select，提供成本机场自动、地区链、共享家宽和全部实际节点。没有新增组、探针池或自动回退链，现有名称保持，缓存选择继续有效。

默认不换路和用户主动选择自动链分别验证。AI无需全局模式即可临时改走机场名称3，并可独立选择地区、其他机场节点或共享家宽。Tunnel自动链复用网页探针，其存在不表示已验证7844/QUIC健康判断；用户可自行权衡。主规则、DNS、自动池、机场与地区顺序保持。

GLOBAL原有的实际节点选项保持。补充“选择实际节点并切换global模式、结束后切回rule”的操作说明，验证全局单节点TCP/UDP出站、节点选择跨重启保留及返回规则后的分流恢复。运行模式与组内手选是两个独立设置。

两份配置的静态设计检查、各7类原生生命周期检查、各518次Tunnel/GLOBAL TCP/UDP检查均通过。覆盖AI默认故障不换路、人工覆盖机场/地区/节点、共享家宽重启保留，Tunnel自动与手选、端点及端口边界、不支持UDP的直接和嵌套手选节点拒绝，以及GLOBAL单节点覆盖与回规则恢复。测试核心串行、无失败重跑；Tunnel测试使用真实规则快照与回环出口，不证明真实线路的QUIC健康或吞吐。

完整展开只有Highest_policy与AI/Tunnel两个入口变化，默认首项、其他业务、规则、DNS和自动链完全保持。GLOBAL原选项未修改。当前仍为111/146组、32个可见入口。[本轮证据](/tmp/rules-select-override-h2l13b7u/manifest.json)。正式配置与WRT未切换。

## 前轮：恢复自建/家宽快捷手选

两份候选各增加一个“自建/家宽节点”select，沿用正式配置的home_only筛选式，全部订阅参与、筛空REJECT。普通、高要求、成本、默认直连业务、Emby及GLOBAL增加该候选；只有主动选择此入口的业务共享组内节点选择。AI、台湾限定、Cloudflare Tunnel候选范围保留，未新增自动池或回退。

当前三/四机场分别111/146组、32个可见select。完整展开对照确认只新增筛选字段、一个手选组及引用，原默认、机场/地区顺序、规则、DNS与检查参数均未变。两份配置的静态设计检查及各6类原生生命周期验证均通过，包括新增的跨业务共享、GLOBAL、重启保留、故障不换路与空池拒绝。核心串行执行、无失败重跑；自动链完整展开保持等价，本轮未重复19类健康回退矩阵。[展开差异](/tmp/rules-home-shortcut-yeehuep_/semantic-diff.json)。[文件摘要与原生结果](/tmp/rules-home-shortcut-yeehuep_/manifest.json)。正式配置与WRT尚未切换。

## 前轮：机场名称1日本优先

用户进一步明确国际服务的地区适用范围优先于部分建连延迟收益。两份候选的机场名称1通用、GitHub、Cloudflare地区链统一改为日本→香港→新加坡→美国。每份只交换三条列表的前两项；完整YAML展开对照确认其他值完全一致，无新增组、规则或参数。未来家宽与其他机场的地区顺序不依据机场名称1的数据外推调整。

此次不是性能结论翻转：同期Google首次请求日本比香港慢287ms，复用慢38ms，未证明日本更稳定。地区限制的官方例证及适用边界见[比较报告](region-priority-comparison.md)。两份文件的原生回归均通过：各19类设计检查、5类重启与订阅生命周期检查，覆盖较快香港不抢占健康日本、日本失效后的香港回退及恢复、独立CF探针、纯下载跨区择优和手选重启保留。

三机场SHA256：`a39bcf660d54b632bd2a2148179cd4a2527437376a1974f77aea6ef53ad05e82`；四机场：`ea7c4b42712315291baed6ff9f2b0894606c8b6ebe5b9032e3438aaf6d957ee3`。正式配置与WRT运行配置未切换。[本轮展开差异](/tmp/rules-japan-priority-7jj6mlmo/semantic-diff.json)。[测试与文件摘要](/tmp/rules-japan-priority-7jj6mlmo/manifest.json)。

生命周期测试保留原60/30/18/45秒周期与3500ms/3次参数。三/四机场模拟日本CONNECT拒绝后，分别约14.8/5.9秒落到香港；恢复后分别约30.0/30.1秒返回日本。仅少节点回环模拟立即拒绝，不代表真实线路恢复上限；所有核心串行执行，无失败重跑。

## 前轮：香港、日本、新加坡地区顺序实测

WRT独立实例累计完成541次检查/请求：此前港日322次；追加新加坡后再做99次三地初筛，以及90次原生复测、30次Google首次/复用请求。扩展阶段五轮同期对照中，机场名称1港/日/新Google原生成功中位数为414/696/950ms，成功次数5/5、5/5、4/5；复用请求为116/154/230ms，各5/5成功。没有证实香港更不稳定，也没有出现足以替换它的新加坡默认路径。

因此未触发整体调序条件，两份候选及正式配置完全未改，机场名称1保持香港→日本→新加坡→美国，机场名称3保持既有日本→新加坡→香港→美国。机场名称3代表日本节点成功时较快，但有超时，不能推广成整个地区更稳定，也未据此扩大黑名单。两次临时实例与节点缓存已清理，生产配置及原进程身份保持。详见[三地比较](region-priority-comparison.md)与[证据摘要](/tmp/rules-hk-jp-qeqx1kyh/manifest.json)。

## 前轮：WRT 真机对照与 Tunnel DNS 修复

已连接用户提供的 WRT，确认 cloudflared 2026.7.3、Mihomo `alpha-ge183c58` 与 MosDNS 同机。生产系统 DNS 把两个 region 各压成一个 Fake-IP；日志明确四条 HA 被限制为两条，指标和现有两条 HTTP/2 连接相符。本轮 188 次无注册 QUIC/TCP+TLS 握手覆盖原始直连、14 个固定节点、生产接管路径、五轮交错复测和 Go/TLS 对照，支持继续保留 DIRECT 默认。日本 HY2 的成功样本更快但出现超时；具体数据和结论边界见[专项记录](cloudflare-tunnel-uplink.md)。

两份候选仅增加两个 region 的精确 real-ip 例外及 `+.argotunnel.com` 国内 DIRECT DoH policy，其余完整展开后与本轮修改前逐项等价。规则、业务组、回退顺序、TUN、嗅探与 IPv6 设置没有新增变化。真实 IP 例外不扩大到整个 argotunnel/cftunnel。

| 本轮验证 | 三机场 | 四机场 |
|---|---|---|
| 静态设计与完整展开差异检查 | 通过 | 通过 |
| 原生 DNS 策略、region 多地址、SRV/TXT、缓存及重启 | 10 类通过 | 10 类通过 |
| Tunnel TCP/UDP 分流、端口边界、手选及重启 | 437 次通过 | 437 次通过 |

WRT 的独立临时核心也验证修复后两个 region 各十个真实 IPv4、AAAA 为空，其他 argotunnel 域仍 Fake-IP。首次尝试 Ruby resolv 时设备缺少该组件，改用现有 nslookup 完成。没有重启生产 connector，因此未把“四条注册连接已恢复”列为通过；也没有持续负载、业务吞吐或全天稳定性结论。当前 Fake-IP 生产路径能够完成 QUIC，历史超时原因未确定，不能归咎为其恒定阻断 UDP。

本轮本地官方核心串行执行；未重复与 DNS 改动无关的完整健康回退、业务域名或 HTTP 订阅生命周期矩阵。生产四个配置文件哈希及原 cloudflared/Mihomo 进程身份在测试前后相同，临时核心、探测器、节点缓存副本与监听端口已清理。

该轮完成时三/四机场分别 776/849 行、110/145 组，均 31 个可见 select、131 条主规则、93 个规则 provider。三机场 SHA256：`9013c0ba9319547e7cdf1127ed3b258f6f2477a36b52c4b7e60115d0d5d69750`；四机场：`a6b65d79c1dd95848c7d58be56b016ff50327ca5a84cdf985d7adbb21700d84e`。修复前快照、真实数据、探测源码、回归结果及摘要见[本轮证据](/tmp/rules-tunnel-live-oiak1rpu/manifest.json)。以下记录保留各轮当时的范围与结果。

## 前轮：Cloudflare Tunnel连接出站

研究与边界见[专项记录](cloudflare-tunnel-uplink.md)。两份候选新增一个Cloudflare Tunnel手选组，默认DIRECT、其余为实际节点；限五个精确域名或全球40个精确端点IP的7844。选择规则后同条件REJECT防止不支持UDP的手选节点漏到其他出口。无新增测速池，既有自动组、业务默认、DNS/TUN和443分流保持。

该轮完成时三/四机场分别110/145组，31个可见select，131条主规则，93个规则provider。三机场SHA256为`8a2ccf51af53cca8fec38fc940357080e136d2fe996f18e6d7c3cd7b560191f6`；四机场为`d37eca55fab5c8ebc1629620cef305fb94a4ea4a6ead63686b3361db24a9d85b`。

[该轮证据](/tmp/rules-tunnel-validation-vvytxzm_)记录原生缺口复现、回归及当时配置摘要：

| 验证 | 三机场 | 四机场 |
|---|---|---|
| 新增项以外完整展开逐项对照、设计静态检查 | 通过 | 通过 |
| 原97域名业务/默认及隔离回归 | 266次通过 | 266次通过 |
| 原UDP选路回归 | 116项通过 | 116项通过 |
| Tunnel专项：40地址、5域名、端口边界、手选隔离、重启、不支持UDP节点 | 437次通过 | 437次通过 |

核心全部串行，Tunnel专项每份包含TCP217次、UDP220次。临时夹具开启IPv6测试端点字面量；候选模板的全局/DNS IPv6仍关闭，未验证真机IPv6接管。内存副本移除两条REJECT的负面对照中，HTTP-only手选节点UDP实际漏回DIRECT，测试准确报错。最初AND末尾附加no-resolve不受内核支持，已移入RULE-SET子条件；专用测试的模块名遮蔽也已修正，初败日志保留。

以上是本机回环规则与缓存验证，没有真实被穿透机、节点、QUIC或业务性能排名；既有19类完整健康回退和5类HTTP订阅生命周期未重新执行。原自动结构未变，新手选组重启由专项覆盖。下方命名与拆分记录属于历史版本，摘要和报告保持原件。

## 前轮：统一机场命名

两份配置统一使用`机场名称1`至`机场名称4`。链路入口、隐藏节点池、子规则及引用同时改名，例如`机场名称1优先-自动`、`日本·机场名称1优先`、`AI-机场名称1-日本`和`GitHub-机场名称3-日本`。`Airport_01`等provider键与既有节点显示前缀保持。

三机场改名29个策略组、12个rematch入口和12个子规则；四机场分别40、18、18。YAML完整展开后与改名前配置逐项比较，仅名称映射不同，路由、默认顺序、过滤、探针与参数均等价；没有增删组或兼容别名。

命名完成时三机场SHA256：`262bb4f9d7ddeb0d095b40bd1995bef49e0289796f1d4fd008d001a4ca07ce2b`。
命名完成时四机场SHA256：`c5ec9ca62abadf0ec21a53427b151106cee6d4ce980228b33abcfb3fd66553e9`。
命名验收目录：[本轮记录](/tmp/rules-naming-validation-yr1g4s61)。

| 本次验证 | 三机场 | 四机场 |
|---|---|---|
| 完整YAML名称映射对照、设计静态检查 | 通过 | 通过 |
| 真实公开规则分流 | 266次请求通过 | 266次请求通过 |
| UDP选路 | 116项通过 | 116项通过 |
| 重启与HTTP订阅生命周期 | 5类通过，12个业务选择保留 | 5类通过，12个业务选择保留 |

三机场节点过滤和规则更新专项也已通过；这些辅助脚本本轮仍仅按三机场执行。官方内核测试串行运行。此次未重复执行19类完整健康回退矩阵；原周期和节点池结构由逐项对照保证未变，生命周期测试仍使用配置原周期。`manifest.json`记录当前文件和报告摘要。初次真实规则测试在脚本名称尚未同步完整时因旧期望失败，其日志保留；同步后最终两份测试均通过。

改名会使缓存中的相应旧选项失效，首次加载应核对机场偏好、地区和AI子池内的手选节点；重新选择后的名称仍跨重启保留。具体节点名称未变。详见[换版边界](single-config-design.md#6-独立文件与换版边界)。

以下是此前拆分的行为与测试证据，说明文字采用当前命名；历史报告和摘要保留当时原件，不作为本次重新执行的结果。

## 上轮：配置拆分与入口简化

- 三机场High/Normal直接默认`机场名称1优先-自动`，Cost直接默认`机场名称3优先-自动`。三个重复层级自动入口删除，内部政策锚点只定义首项与候选范围。
- 三机场高要求/普通地区别名合并为`日本·机场名称1优先`等，成本地区改为`日本·机场名称3优先`等；地区范围及跨机场顺序保持。
- 四机场High直接默认`机场名称2优先-自动`，地区对应机场名称2优先；普通与成本沿用机场名称1优先／机场名称3优先，未额外扩充它们的选项。
- 三机场删除Airport_02及整套注释模板。四机场文件中的provider、池和子规则已完整展开，只需填写实际订阅和连接参数；目前不存在真实机场名称2。
- 两文件完全独立，运行不依赖生成器或基础文件。公共修改须同步两份，静态检查核对公共规则、系统设置、筛选与现有订阅参数。

| 指标 | 拆分前 | 三机场文件 | 四机场文件 |
|---|---:|---:|---:|
| 可见select | 30 | 30 | 30 |
| 全部策略组 | 三109／四144 | 109 | 144 |
| rematch入口 | 20 | 12 | 18 |
| 子规则 | 三14／四19 | 12 | 18 |
| 主规则／规则provider | 127／92 | 127／92 | 127／92 |
| 配置行数 | 852（含注释模板） | 717 | 790 |

删除的是重复路由入口，所有非select节点池的类型、成员、URL、筛选、周期和顺序与各自原变体完全一致。主规则和92个规则provider逐项不变。四机场Airport_02仅将原订阅占位提示统一为“订阅链接2”，其他provider字段保持。

拆分后、命名统一前三机场SHA256：`3060b116ab77795589c8702b4a931966b5aba5c7e0cf69020339af428c1b844c`。
拆分后、命名统一前四机场SHA256：`d8aa1de48bc066dc1da7fbec7b12fb858ca3a2b9a84fbf4e038c62029f869da3`。
拆分前候选SHA256：`35de645e33a2a840669252eeeeb70cb23b4ea26be860f61e41cc21caedf37280`。

## 保持的行为

三机场高要求/普通仍1→3→4，成本仍3→1→4；四机场分别2→1→3→4、1→2→3→4、3→1→2→4。Google/FCM保持普通，Microsoft/OneDrive及境外通信保持此前复评的成本。AI仅机场名称1手选，默认直连、金融、独立严格媒体、哔哩东南亚、Emby及其他特殊入口均保留。

机场名称1自动原本包含纯下载特殊分支，不能仅用名称替换断言全部子规则相同。本轮保留`pure_download_domain`两个精确域的机场名称1跨区择优，普通GitHub仍遵循机场内地区顺序；CF仍按已选业务链路决定探针。主规则先将纯下载归入独立入口，所以按实际可达业务路径比较默认与手选出口，而非声称删除前后子规则字面完全相同。

## 上轮验证

证据目录：[拆分验收](/tmp/rules-split-validation-51m59y8e)。`manifest.json`记录两份配置、脚本、对照输入及结果摘要。核心测试串行运行，直接读取实际文件，不从注释生成四机场配置。

| 范围 | 三机场 | 四机场 | 报告 |
|---|---|---|---|
| 默认/手选静态对照 | 通过 | 通过 | `equivalence-three/four.json` |
| 原生设计与健康回退 | 109组、19类通过 | 144组、19类通过 | `design-three/four.json` |
| 真实公开规则 | 266请求通过 | 266请求通过 | `real-three/four-final.json` |
| UDP选路 | 116项通过 | 116项通过 | `udp-three/four.json` |
| 重启与HTTP订阅 | 5类通过，12入口选择保留 | 5类通过，12入口选择保留 | `lifecycle-three/four.json` |

每份文件各对照18,076个域名见证和127,772条映射后的手选链路，默认终结池和手选终结池均零差异，3,201个DIRECT见证保持。节点池及每个业务候选列表经明确名称映射后逐项相同。静态比较不执行IP条件、不穷举通配符、不模拟健康或缓存；实际节点由未变的use/filter定义约束。

真实规则每份包含97个域名的业务归属/默认探针两阶段，以及72次选择隔离，共266请求。规则快照未重下：89份下载原件、1份同源本地Google回退、2份内联集合；清单SHA256为`e88e01e44cc97ea2620e95d063f549bcccfed65911cf6535b5cfb08411d481d3`。

真实规则与UDP夹具用回环标签验证路由；设计测试缩短周期验证健康传播，并补充纯下载机场名称1跨区择优与普通GitHub地区顺序的直接比较。生命周期保留60/30/18/45秒周期、3500ms及3次失败参数，验证新选择名在重启与订阅变化中的行为。辅助AI/DNS/节点筛选/规则更新专项未扩改或冒充双文件新执行；未变结构及隐藏链由本轮静态对照与设计测试约束。

## 缓存与健康边界

旧层级默认别名失效后，会回到等价的新默认首项；旧地区选项改名后不能自动映射，可能回到默认地区，首次换版应重新检查地区偏好。上述结论针对拆分时的名称；本次命名统一又变更了机场偏好、地区和AI子池名称，缓存影响以本文开头及设计说明为准。实际节点及Emby专属子池名称未变。新版同名有效选择正常重启恢复；切换到四机场文件是显式操作，不能承诺旧三机场选择自动转换为新的家宽默认。

两份文件的生命周期各通过5类检查、12个入口选择保留，四机场高要求默认/地区实际命中机场名称2，AI前后仍走机场名称1。本轮保留生产检查周期的单次计时如下，计时对象均为Google的机场名称1优先链：

| 事件 | 三机场 | 四机场 |
|---|---:|---:|
| 初次日本地区请求命中 | 20.06秒 | 20.05秒 |
| 故障后首次命中同机场下一地区 | 14.63秒 | 14.64秒 |
| 恢复后首次重新命中首选地区 | 30.04秒 | 15.01秒 |

这不是持续稳定时长或公网恢复上限；不同检查时刻产生的计时差异不能解释为四机场更快。两次最初三个业务请求均成功；拒绝握手分别14/10次，包含健康探测，不代表用户失败次数。现有日志快照保存在`lifecycle-three/four-core-live.log`。健康状态传播期间可能短暂跨机场或出现失败，仍不能验证真实同步、推送或语音质量。既有[默认层级复评](/tmp/rules-default-review-validation-j9sguw19/manifest.json)与[聚合验收](/tmp/rules-risk-grouping-validation-1r_za_2z/manifest.json)的配置摘要和失败证据保持原样。本轮沿用UDP源端口保留修复；旧75秒冷启动异常仍未被认定修复。

本轮没有真实家宽/机场解锁、账号风控、FCM到达率、OneDrive同步、语音、公网UDP/QUIC、实机TUN或耗电测试。探针只判断连接状态，不能判断验证码、付费权限或吞吐；应用失败而探针正常时仍可能不自动回退。

复现时逐项串行运行；四机场将文件参数替换为`cinfigfull_new_4.yaml`：

```sh
python scripts/test-config-design.py --config configfull_new.yaml
python scripts/test-real-rule-routing.py --config configfull_new.yaml --rules-dir /tmp/rules-public-audit-dw354gk_/remote
python scripts/test-rematch-udp.py --config configfull_new.yaml
python scripts/test-runtime-lifecycle.py --config configfull_new.yaml
```
